'use client';
import BookmarkIcon from '@/assets/svgs/bookmark.svg';

const SaveButton = () => {
  return (
    <button className="bg-white border-0 flex-shrink-1">
      <BookmarkIcon />
    </button>
  );
};

export default SaveButton;
